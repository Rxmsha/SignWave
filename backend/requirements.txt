django
opencv-python
# Pinned: mediapipe >=0.10.18 removed the legacy mp.solutions API (holistic/hands)
# that this project relies on. 0.10.14 keeps it and works with protobuf 4.x.
mediapipe==0.10.14
djangorestframework
django-cors-headers
torch==2.12.1
torchvision==0.27.1
numpy<2.1
# Pinned: tensorflow 2.21 requires protobuf>=6, which conflicts with mediapipe's
# protobuf 4.x. 2.18 keeps the whole ML stack on protobuf 4.x.
tensorflow==2.18.0
protobuf>=4.25.3,<5
gradio
transformers
